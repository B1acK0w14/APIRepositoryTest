# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

def general_pods
  pod "SnapKit"
end

target 'APIRepositoryTest' do
  # Pods for APIRepositoryTest
  general_pods

end
